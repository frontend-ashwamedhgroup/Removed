MODEL CLASS LABEL STATUS
========================

Updated model output count: 161
Active class_names.json: NOT INCLUDED, intentionally.

Reason: the attached .keras file does not contain the label names or their
index order. Reusing the old 38-class mapping would make the website display
incorrect crop/disease names even if the numerical model prediction were right.

Use install_updated_model_labels_windows.bat with the class_names.json generated
by the same training run, or with the exact 161-folder training dataset.
